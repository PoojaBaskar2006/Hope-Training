class Solution {
    public int numWaterBottles(int numBottles, int numExchange) {
        int drank=numBottles;
        while(numBottles>=numExchange){
            int q=numBottles/numExchange;
            drank+=q;
            numBottles=q+(numBottles-(numExchange*q));
        }
        return drank;
    }
}
